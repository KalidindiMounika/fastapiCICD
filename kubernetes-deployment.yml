apiVersion: v1
kind: Deployment
metadata:
  name: python-cluster
  labels:
    app: python-cluster
spec:
  containers:
    -name: flaskapi
      image: mounika30/python-flask:latest
      ports:
        -name: api-port
          containerPort: 8080
          protocol: TCP

