apiVersion: v1
kind: Pod
metadata:
  name: api
  labels:
    app: api
spec:
  containers:
    -name: api
      image: mounika30/python-flask:latest
      ports:
        -name: api-port
          containerPort: 3000
          protocol: TCP
