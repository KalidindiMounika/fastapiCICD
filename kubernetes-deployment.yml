apiVersion: v1
kind: Deployment
metadata:
  name: python-cluster
spec:
  replicas: 3
  selector:
    matchLabels:
      app: python-cluster
  template:
    metadata:
      labels:
        app: python-cluster
spec:
  containers:
    -name: python-cluster
      image: mounika30/python-flask:latest
      ports:
        -name: api-port
          containerPort: 5000
          protocol: TCP




